using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ImageDisplay : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//Todo: instantiate an array of images from the player to be used during gameplay
		//Then display the default image during gameplay, only to be updated when
		//specific events occur.
	}



	// Update is called once per frame
	void Update () {
		//Todo: create a switch statement that changes which image is active on the screen
		//i.e. based on whether or not the player has been hit, has died, or finished the level
	}
}
